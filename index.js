const math = require('./math.js');

console.log(math); //para ver que contiene el modulo math, este muestra que es un objeto y tiene multipleas propiedades (funcion); 


//console.log(math.add(1,0),math.subtract(2,1), math.multiply(2,2),math.divide(2,0) ,math.divide(4,2));

